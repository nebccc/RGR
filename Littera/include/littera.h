#pragma once

#include <iostream>
#include <string>
#include <map>
#include <locale>

using namespace std;

extern map<char, char> eng_alphabet;
extern map<char, char> eng_alphabet;
string init_littera();
void write_text(const string& text);
void littera();